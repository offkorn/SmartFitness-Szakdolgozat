<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:SmartFitness.Converters"
             BackgroundColor="#F6FAFD"
             x:Class="SmartFitness.Pages.WorkoutSearchPage"
             Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <converters:NullToDefaultImageConverter x:Key="NullToDefaultImageConverter" />
        <converters:GoalsToStringConverter x:Key="GoalsToStringConverter" />
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, *">

        <VerticalStackLayout Grid.Row="0" Padding="0,10" Spacing="0">


            <!-- tab bar, - back button and page title -->
            <HorizontalStackLayout Spacing="0" Margin="0,0,0,0">

                <ImageButton Source="back_icon" Scale="0.35" Rotation="180" HorizontalOptions="Start" Clicked="OnBackClicked" VerticalOptions="Center"></ImageButton>

                <Label 
                   Text="Search Workouts"
                   FontSize="Medium"
                   FontAttributes="Bold"
                    VerticalTextAlignment="Center" HorizontalOptions="Center"/>
            </HorizontalStackLayout>

            <!-- Search -->
            <Frame BackgroundColor="#F6FAFD"
                   BorderColor="#E2E2E2"
                   HorizontalOptions="FillAndExpand"
                   CornerRadius="15"
                   Padding="0,0" 
                   MinimumHeightRequest="50"
                   Margin="20,20,20,20" 
                   HasShadow="True">
                <Frame.Shadow>
                    <Shadow Brush="#E8EBEE" Offset="1,4" Radius="0"/>
                </Frame.Shadow>

                <Grid ColumnDefinitions="*, Auto">
                    <Entry x:Name="SearchEntry"
                           Placeholder="Type in"
                           PlaceholderColor="Gray"
                           TextColor="Black"
                           VerticalOptions="Center"
                           Margin="15,0,0,0"
                           FontSize="Body"
                           ReturnCommand="{Binding SearchCommand}"
                           TextChanged="OnTextChanged"/>


                    <Image Margin="0,0,20,0"
                           HorizontalOptions="End"
                           VerticalOptions="Center"
                           Source="search_icon.png" 
                           Scale="0.7"/>

                    <Button Grid.Column="1" 
                         Text="x" 
                    BackgroundColor="Transparent"
                         HeightRequest="20" 
                         WidthRequest="20" 
                         Margin="0,0,10,0"
                         Clicked="OnClearSearchClicked"
                         IsVisible="False"
                         x:Name="ClearButton"/>

                </Grid>
            </Frame>

        </VerticalStackLayout>

        <CollectionView Grid.Row="1" x:Name="ResultsCollectionView" Margin="0,10,0,0"
                        RemainingItemsThreshold="5"
                        RemainingItemsThresholdReached="OnLoadMoreItems">

            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10">
                    <Label Text="Start typing to search..." TextColor="Gray" FontSize="Body" HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="1" VerticalItemSpacing="5" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border StrokeShape="RoundRectangle 15"
                            StrokeThickness="0"
                            BackgroundColor="White"
                            Margin="15,6"
                            Padding="10">

                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnResultTapped" />
                        </Border.GestureRecognizers>

                        <Grid ColumnDefinitions="Auto, *">

                            <Border Grid.Column="0" 
                                    StrokeShape="RoundRectangle 30"
                                    HeightRequest="60"
                                    WidthRequest="60"
                                    StrokeThickness="0"
                                    BackgroundColor="Transparent"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center">
                                
                                <Image Source="{Binding ImageUrl}" 
                                       Aspect="AspectFit" 
                                       HeightRequest="45" 
                                       WidthRequest="45"
                                       HorizontalOptions="Center" 
                                       VerticalOptions="Center"/>
                            </Border>

                            <Grid Grid.Column="1" RowDefinitions="Auto" ColumnDefinitions="*, Auto" Margin="15,0,0,0" VerticalOptions="Center">

                                <VerticalStackLayout Spacing="0">
                                    <Label Grid.Row="0" Grid.Column="0" 
                                           Text="{Binding Title}" 
                                           FontAttributes="Bold" 
                                           FontSize="Medium" 
                                           TextColor="Black" 
                                           LineBreakMode="TailTruncation"
                                           VerticalOptions="Center"
                                           MaxLines="1"/>

                                    <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                           Text="{Binding Subtitle}" 
                                           TextColor="Gray" 
                                           FontSize="Small" 
                                           Margin="0,4,0,0"/>
                                </VerticalStackLayout>
                                

                                <Border Grid.Row="0" Grid.Column="1" 
                                        StrokeShape="RoundRectangle 5" 
                                        BackgroundColor="{Binding TypeColor}"
                                        Padding="8,4"
                                        StrokeThickness="0"
                                        VerticalOptions="Center"
                                        HorizontalOptions="End"
                                        Margin="5,0,0,0">
                                    <Label Text="{Binding TypeText}" 
                                           TextColor="White" 
                                           FontSize="10" 
                                           FontAttributes="Bold"/>
                                </Border>

                                
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            
        </CollectionView>
    </Grid>
</ContentPage>